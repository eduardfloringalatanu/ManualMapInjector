# ManualMapInjector
 
A small project for GoldSrc engine, wich provides the ability to inject arbitrary modules using manual mapping. Additionally, the injector is using thread hijacking in order to increase its stealth furthermore.

# How to use?

Simply place all DLLs in the folder named "modules", make sure the game is already running, then start the injector.
